const nodemailer = require('nosemailer')

const sendEmail = options =>{
    // 1) Create a trasporter
    const transporter = nodemailer.createTransport({
        Service:''

    })

    // 2) Define the email options

    // 3) Actually send the email 

}